# SPK CyberLab — Образовательная платформа

SPK CyberLab — это интерактивная образовательная среда для изучения информационной безопасности, криптографии, баз данных и программирования. Разработана для студентов и преподавателей БПОУ ОО «Сибирский профессиональный колледж».

**Веб-сайт:** [spk-cyberlab.ru](https://spk-cyberlab.ru)  
**Год разработки:** 2026  
**Разработчик:** Стариков А.В.

## Функциональные возможности

- Ролевая модель: Гость, Студент, Преподаватель, Администратор.
- Аутентификация и регистрация: студенты регистрируются по группе и номеру в списке; логин генерируется автоматически.
- Курсы, модули, темы, задания: иерархическая структура контента.
- Преподавательский интерфейс: создание и редактирование курсов, модулей, тем, заданий.
- Выполнение заданий: автоматическая проверка текстовых ответов по шаблону.
- Административная панель: управление группами, студентами, преподавателями, просмотр всех данных.
- Мемы (в разработке): лента IT-юмора с модерацией.
- Технический стек: React + TypeScript + Tailwind (Frontend), Node.js + Express + Prisma (Backend), PostgreSQL (БД), Docker.

## Технологический стек

| Компонент | Технология |
| ---------- | ------------ |
| Frontend | React, TypeScript, Vite, Tailwind CSS |
| Backend | Node.js, Express, TypeScript |
| ORM | Prisma |
| База данных | PostgreSQL (в Docker) |
| Аутентификация | JWT, bcrypt |
| Контейнеризация | Docker, Docker Compose |
| Хостинг | Timeweb.cloud (VPS) |

---

## Структура проекта

```txt
spk-cyberlab.ru/
├── backend/
│ ├── src/
│ │ ├── controllers/
│ │ ├── routes/
│ │ ├── middleware/
│ │ ├── utils/
│ │ └── index.ts
│ ├── prisma/
│ │ └── schema.prisma
│ ├── Dockerfile.dev
│ ├── package.json
│ └── .env
├── frontend/
│ ├── src/
│ │ ├── pages/
│ │ ├── components/
│ │ ├── hooks/
│ │ ├── services/
│ │ ├── App.tsx
│ │ └── main.tsx
│ ├── public/
│ ├── package.json
│ └── tailwind.config.js
├── docker-compose.yml
└── README.md
```

## Тестовые учётные записи

| Роль | Логин | Пароль |
| ------ | ------- | -------- |
| Студент (пример) | ip224-01 | student123 |
| Преподаватель | teacher1 | 000000 |
| Администратор | admin | admin123 |

> Студенческий логин генерируется по шаблону: `{префикс_группы}-{номер_по_списку}`. Например, для группы `ИП224` и номера `1` логин будет `ip224-01`.

## Запуск проекта (локальная разработка)

### 1. Клонирование репозитория

```bash
git clone <url>
cd spk-cyberlab.ru
```

### 2. Запуск с Docker Compose

Убедитесь, что Docker и Docker Compose установлены.

```bash
docker-compose up -d --build
```

После запуска:

Frontend: <http://localhost:5173>

Backend API: <http://localhost:5000/api/health>

PostgreSQL: доступен на порту 5432 (пароль prisma123)

### 3. Ручной запуск (без Docker)

Backend

```bash
cd backend
npm install
cp .env.example .env   # настройте переменные
npx prisma migrate dev
npm run dev
```

Frontend

```bash
cd frontend
npm install
npm run dev
```

### Что реализовано (на текущий момент)

- Полноценная аутентификация и ролевая модель
- Регистрация студентов через группу и номер (автогенерация логина)
- Админ-панель (группы, студенты, преподаватели)
- Управление курсами (создание, редактирование, удаление)
- Иерархия: курс → модуль → тема → задание
- Редактор курса для преподавателей (добавление модулей, тем, заданий)
- Выполнение заданий студентами (текстовый ответ, автопроверка)
- Страница курсов для студентов (просмотр)
- Единый макет с логотипом и футером
- Использование шрифта Cascadia Code

### В планах (ближайшее будущее)

- Раздел мемов (загрузка, модерация, голосование)
- Система достижений и геймификация
- Полигон-песочница (SQL-инъекции, XSS и др.)
- Аналитика и статистика для студентов и преподавателей
- Две темы оформления (светлая/тёмная)

## Контакты

По всем вопросам обращайтесь к разработчику:
**Стариков Александр Владимирович (преподаватель БПОУ ОО «Сибирский профессиональный колледж»)**

> (c) 2025–2026 БПОУ ОО «Сибирский профессиональный колледж». Все права защищены.
