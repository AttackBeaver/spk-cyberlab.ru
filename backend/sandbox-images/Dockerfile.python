# Базовый образ Python 3.11 slim
FROM python:3.11-slim

# Создаём непривилегированного пользователя
RUN addgroup --system --gid 1001 student && \
    adduser --system --uid 1001 --gid 1001 student

# Устанавливаем рабочий каталог
WORKDIR /app

# Ограничиваем права на запись (только чтение для пользователя)
RUN chown -R student:student /app && chmod -R 755 /app

# Переключаемся на непривилегированного пользователя
USER student

# Устанавливаем ограничения времени выполнения (будет переопределено при запуске)
# Ограничиваем доступ к сети — запрещаем всё
# Ограничиваем доступ к файловой системе — только /app

CMD ["python3"]